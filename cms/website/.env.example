VITE_API_URL=http://localhost:4009/api/v1

# Fallback <head> baked into index.html at build time, for clients that do not
# run JavaScript. Real per-page values come from the CMS at runtime; scrapers get
# them from /api/v1/public/share-preview. Changing these needs a rebuild.
VITE_SITE_NAME=THI-O TOGETHER
VITE_SITE_DESCRIPTION=
VITE_SITE_IMAGE=
VITE_SITE_URL=https://tt.dna.co.th
